import { LoginContent } from "./_components/LoginContent";

export default function LoginPage() {
  return <LoginContent />;
}

export const metadata = {
  title: "Login - JuiceVault",
  description: "Sign in to access the ultimate collection of unreleased Juice WRLD tracks",
};